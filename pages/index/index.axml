<view class="container">
  <text class="title">基础图表示例</text>
  <view a:for="{{charts}}" a:for-item="chart" a:key="{{chart.name}}"
    data-page="{{chart.name}}"
    onTap="gotoPage"
    class="list-item">
    {{chart.value}}
  </view>
</view>
